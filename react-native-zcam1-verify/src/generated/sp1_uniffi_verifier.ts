// This file was autogenerated by some hot garbage in the `uniffi-bindgen-react-native` crate.
// Trust me, you don't want to mess with it!
import nativeModule, {
  type UniffiRustFutureContinuationCallback,
  type UniffiForeignFuture,
  type UniffiForeignFutureStructU8,
  type UniffiForeignFutureCompleteU8,
  type UniffiForeignFutureStructI8,
  type UniffiForeignFutureCompleteI8,
  type UniffiForeignFutureStructU16,
  type UniffiForeignFutureCompleteU16,
  type UniffiForeignFutureStructI16,
  type UniffiForeignFutureCompleteI16,
  type UniffiForeignFutureStructU32,
  type UniffiForeignFutureCompleteU32,
  type UniffiForeignFutureStructI32,
  type UniffiForeignFutureCompleteI32,
  type UniffiForeignFutureStructU64,
  type UniffiForeignFutureCompleteU64,
  type UniffiForeignFutureStructI64,
  type UniffiForeignFutureCompleteI64,
  type UniffiForeignFutureStructF32,
  type UniffiForeignFutureCompleteF32,
  type UniffiForeignFutureStructF64,
  type UniffiForeignFutureCompleteF64,
  type UniffiForeignFutureStructPointer,
  type UniffiForeignFutureCompletePointer,
  type UniffiForeignFutureStructRustBuffer,
  type UniffiForeignFutureCompleteRustBuffer,
  type UniffiForeignFutureStructVoid,
  type UniffiForeignFutureCompleteVoid,
} from "./sp1_uniffi_verifier-ffi";
import {
  type UniffiByteArray,
  AbstractFfiConverterByteArray,
  FfiConverterArrayBuffer,
  FfiConverterBool,
  FfiConverterInt32,
  RustBuffer,
  UniffiError,
  UniffiInternalError,
  UniffiRustCaller,
  uniffiCreateFfiConverterString,
  uniffiTypeNameSymbol,
  variantOrdinalSymbol,
} from "uniffi-bindgen-react-native";

// Get converters from the other files, if any.
const uniffiCaller = new UniffiRustCaller(() => ({ code: 0 }));

const uniffiIsDebug =
  // @ts-ignore -- The process global might not be defined
  typeof process !== "object" ||
  // @ts-ignore -- The process global might not be defined
  process?.env?.NODE_ENV !== "production" ||
  false;
// Public interface members begin here.

/**
 * Wrapper around [`sp1_verifier::CompressedVerifier::verify_sp1_proof`].
 *
 * We hardcode the Plonk VK bytes to only verify SP1 proofs.
 */
export function verifyCompressed(
  proof: ArrayBuffer,
  publicInputs: ArrayBuffer,
  sp1VkHash: ArrayBuffer,
): boolean /*throws*/ {
  return FfiConverterBool.lift(
    uniffiCaller.rustCallWithError(
      /*liftError:*/ FfiConverterTypeError.lift.bind(FfiConverterTypeError),
      /*caller:*/ (callStatus) => {
        return nativeModule().ubrn_uniffi_sp1_uniffi_verifier_fn_func_verify_compressed(
          FfiConverterArrayBuffer.lower(proof),
          FfiConverterArrayBuffer.lower(publicInputs),
          FfiConverterArrayBuffer.lower(sp1VkHash),
          callStatus,
        );
      },
      /*liftString:*/ FfiConverterString.lift,
    ),
  );
}
/**
 * Wrapper around [`sp1_verifier::Groth16Verifier::verify`].
 *
 * We hardcode the Groth16 VK bytes to only verify SP1 proofs.
 */
export function verifyGroth16(
  proof: ArrayBuffer,
  publicInputs: ArrayBuffer,
  sp1VkHash: string,
): boolean /*throws*/ {
  return FfiConverterBool.lift(
    uniffiCaller.rustCallWithError(
      /*liftError:*/ FfiConverterTypeError.lift.bind(FfiConverterTypeError),
      /*caller:*/ (callStatus) => {
        return nativeModule().ubrn_uniffi_sp1_uniffi_verifier_fn_func_verify_groth16(
          FfiConverterArrayBuffer.lower(proof),
          FfiConverterArrayBuffer.lower(publicInputs),
          FfiConverterString.lower(sp1VkHash),
          callStatus,
        );
      },
      /*liftString:*/ FfiConverterString.lift,
    ),
  );
}
/**
 * Wrapper around [`sp1_verifier::PlonkVerifier::verify`].
 *
 * We hardcode the Plonk VK bytes to only verify SP1 proofs.
 */
export function verifyPlonk(
  proof: ArrayBuffer,
  publicInputs: ArrayBuffer,
  sp1VkHash: string,
): boolean /*throws*/ {
  return FfiConverterBool.lift(
    uniffiCaller.rustCallWithError(
      /*liftError:*/ FfiConverterTypeError.lift.bind(FfiConverterTypeError),
      /*caller:*/ (callStatus) => {
        return nativeModule().ubrn_uniffi_sp1_uniffi_verifier_fn_func_verify_plonk(
          FfiConverterArrayBuffer.lower(proof),
          FfiConverterArrayBuffer.lower(publicInputs),
          FfiConverterString.lower(sp1VkHash),
          callStatus,
        );
      },
      /*liftString:*/ FfiConverterString.lift,
    ),
  );
}

const stringConverter = {
  stringToBytes: (s: string) =>
    uniffiCaller.rustCall((status) =>
      nativeModule().ubrn_uniffi_internal_fn_func_ffi__string_to_arraybuffer(
        s,
        status,
      ),
    ),
  bytesToString: (ab: UniffiByteArray) =>
    uniffiCaller.rustCall((status) =>
      nativeModule().ubrn_uniffi_internal_fn_func_ffi__arraybuffer_to_string(
        ab,
        status,
      ),
    ),
  stringByteLength: (s: string) =>
    uniffiCaller.rustCall((status) =>
      nativeModule().ubrn_uniffi_internal_fn_func_ffi__string_to_byte_length(
        s,
        status,
      ),
    ),
};
const FfiConverterString = uniffiCreateFfiConverterString(stringConverter);

// Flat error type: Exception
export enum Exception_Tags {
  Groth16 = "Groth16",
  Plonk = "Plonk",
  Compressed = "Compressed",
}
export const Exception = (() => {
  class Groth16 extends UniffiError {
    /**
     * @private
     * This field is private and should not be used.
     */
    readonly [uniffiTypeNameSymbol]: string = "Exception";
    /**
     * @private
     * This field is private and should not be used.
     */
    readonly [variantOrdinalSymbol] = 1;

    public readonly tag = Exception_Tags.Groth16;

    constructor(message: string) {
      super("Exception", "Groth16", message);
    }

    static instanceOf(e: any): e is Groth16 {
      return instanceOf(e) && (e as any)[variantOrdinalSymbol] === 1;
    }
  }
  class Plonk extends UniffiError {
    /**
     * @private
     * This field is private and should not be used.
     */
    readonly [uniffiTypeNameSymbol]: string = "Exception";
    /**
     * @private
     * This field is private and should not be used.
     */
    readonly [variantOrdinalSymbol] = 2;

    public readonly tag = Exception_Tags.Plonk;

    constructor(message: string) {
      super("Exception", "Plonk", message);
    }

    static instanceOf(e: any): e is Plonk {
      return instanceOf(e) && (e as any)[variantOrdinalSymbol] === 2;
    }
  }
  class Compressed extends UniffiError {
    /**
     * @private
     * This field is private and should not be used.
     */
    readonly [uniffiTypeNameSymbol]: string = "Exception";
    /**
     * @private
     * This field is private and should not be used.
     */
    readonly [variantOrdinalSymbol] = 3;

    public readonly tag = Exception_Tags.Compressed;

    constructor(message: string) {
      super("Exception", "Compressed", message);
    }

    static instanceOf(e: any): e is Compressed {
      return instanceOf(e) && (e as any)[variantOrdinalSymbol] === 3;
    }
  }

  // Utility function which does not rely on instanceof.
  function instanceOf(e: any): e is Exception {
    return (e as any)[uniffiTypeNameSymbol] === "Exception";
  }
  return {
    Groth16,
    Plonk,
    Compressed,
    instanceOf,
  };
})();

// Union type for Exception error type.

export type Exception = InstanceType<
  (typeof Exception)[keyof Omit<typeof Exception, "instanceOf">]
>;

const FfiConverterTypeError = (() => {
  const intConverter = FfiConverterInt32;
  type TypeName = Exception;
  class FfiConverter extends AbstractFfiConverterByteArray<TypeName> {
    read(from: RustBuffer): TypeName {
      switch (intConverter.read(from)) {
        case 1:
          return new Exception.Groth16(FfiConverterString.read(from));

        case 2:
          return new Exception.Plonk(FfiConverterString.read(from));

        case 3:
          return new Exception.Compressed(FfiConverterString.read(from));

        default:
          throw new UniffiInternalError.UnexpectedEnumCase();
      }
    }
    write(value: TypeName, into: RustBuffer): void {
      const obj = value as any;
      const index = obj[variantOrdinalSymbol] as number;
      intConverter.write(index, into);
    }
    allocationSize(value: TypeName): number {
      return intConverter.allocationSize(0);
    }
  }
  return new FfiConverter();
})();

/**
 * This should be called before anything else.
 *
 * It is likely that this is being done for you by the library's `index.ts`.
 *
 * It checks versions of uniffi between when the Rust scaffolding was generated
 * and when the bindings were generated.
 *
 * It also initializes the machinery to enable Rust to talk back to Javascript.
 */
function uniffiEnsureInitialized() {
  // Get the bindings contract version from our ComponentInterface
  const bindingsContractVersion = 29;
  // Get the scaffolding contract version by calling the into the dylib
  const scaffoldingContractVersion =
    nativeModule().ubrn_ffi_sp1_uniffi_verifier_uniffi_contract_version();
  if (bindingsContractVersion !== scaffoldingContractVersion) {
    throw new UniffiInternalError.ContractVersionMismatch(
      scaffoldingContractVersion,
      bindingsContractVersion,
    );
  }
  if (
    nativeModule().ubrn_uniffi_sp1_uniffi_verifier_checksum_func_verify_compressed() !==
    18206
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      "uniffi_sp1_uniffi_verifier_checksum_func_verify_compressed",
    );
  }
  if (
    nativeModule().ubrn_uniffi_sp1_uniffi_verifier_checksum_func_verify_groth16() !==
    59355
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      "uniffi_sp1_uniffi_verifier_checksum_func_verify_groth16",
    );
  }
  if (
    nativeModule().ubrn_uniffi_sp1_uniffi_verifier_checksum_func_verify_plonk() !==
    45965
  ) {
    throw new UniffiInternalError.ApiChecksumMismatch(
      "uniffi_sp1_uniffi_verifier_checksum_func_verify_plonk",
    );
  }
}

export default Object.freeze({
  initialize: uniffiEnsureInitialized,
  converters: {
    FfiConverterTypeError,
  },
});
