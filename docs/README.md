This is a [Vocs](https://vocs.dev) project bootstrapped with the Vocs CLI.

### Running
- The docs is built using vocs.
- Run `npm run dev` to spin up the docs locally
- Run `npm run build` to build the docs for production
