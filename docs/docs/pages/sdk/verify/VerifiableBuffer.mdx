---
title: VerifiableBuffer
description: Class for verifying C2PA manifests and ZK proofs in Node.js
---

# VerifiableBuffer

Class for verifying C2PA manifests and ZK proofs in Node.js. Extracts the manifest on construction and provides methods to verify bindings, proofs, and retrieve metadata.

## Usage

```ts
import { VerifiableBuffer } from "@succinctlabs/zcam1-verify";
import { readFileSync } from "node:fs";

const buffer = readFileSync(filePath);

const verifiable = new VerifiableBuffer(buffer, "image/jpeg");

// Verify hardware-backed bindings (App Attest)
const bindingsResult = await verifiable.verifyBindings(false);

// Verify zero-knowledge proof
const proofResult = await verifiable.verifyProof("TEAM_ID.com.example.app");

// Get capture metadata
const metadataResult = await verifiable.captureMetadata();
```

## Constructor

### new VerifiableBuffer(buffer, format)

Creates a [`VerifiableBuffer`](/sdk/verify/VerifiableBuffer) instance by extracting the C2PA manifest from the file.

### buffer (required)

- **Type:** `Buffer`

The file data as a Node.js Buffer

### type (required)

- **Type:** `string`
The MIME type of the file (e.g. "image/jpeg")

## Methods

### verifyBindings(production)

Verifies the hardware-backed bindings in the C2PA manifest. See [`verifyBindings`](/sdk/verify/verifyBindings).

### verifyProof(appId)

Verifies the zero-knowledge proof in the C2PA manifest. See [`verifyProof`](/sdk/verify/verifyProof).

### dataHash()

Computes the content hash of the file. See [`dataHash`](/sdk/verify/dataHash).

### captureMetadata()

Gets the capture metadata from the C2PA manifest. See [`captureMetadata`](/sdk/verify/captureMetadata).

### authenticityStatus()

Determines the authenticity status of the file. See [`authenticityStatus`](/sdk/verify/authenticityStatus).

### bindings()

Returns the raw `succinct.bindings` assertion data. Returns `ResultAsync<Record<string, any>, Error>`.

### proof()

Returns the raw `succinct.proof` assertion data. Returns `ResultAsync<Record<string, any>, Error>`.

## Example

```ts
import { VerifiableBuffer } from "@succinctlabs/zcam1-verify";
import { readFileSync } from "node:fs";

const buffer = readFileSync("photo.jpg");
const verifiable = new VerifiableBuffer(buffer, "image/jpeg");

const bindingsResult = await verifiable.verifyBindings(false);
const proofResult = await verifiable.verifyProof("TEAM_ID.com.example.app");
const metadataResult = await verifiable.captureMetadata();

if (bindingsResult.isOk() && proofResult.isOk()) {
  console.log("Verified!");
  if (metadataResult.isOk()) {
    console.log("Captured:", metadataResult.value.when);
  }
} else {
  console.log("Verification failed");
}
```

## Notes

- All verification methods return `ResultAsync` from `neverthrow` for error handling
- [`verifyProof()`](/sdk/verify/verifyProof) requires the same `appId` used during [`initCapture()`](/sdk/capture/initCapture)
- Use [`verifyBindings(false)`](/sdk/verify/verifyBindings) for development, `verifyBindings(true)` for production
